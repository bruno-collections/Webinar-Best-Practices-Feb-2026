meta {
  name: 02-Working with Variables
  seq: 2
}

docs {
  # 📦 Section 2: Working with Variables

  Now that you can send requests, let's explore **environments** and **variables** — the backbone of reusable API collections.

  In this section, you will:

  - ✅ Send POST and PUT requests with JSON bodies
  - ✅ Use environment variables (`{{baseUrl}}`, `{{defaultUserId}}`)
  - ✅ Use Bruno's dynamic variables (`{{$randomFullName}}`, `{{$randomEmail}}`)
  - ✅ Switch between environments and see values change

  ---

  ### ✏️ Step 1: Create "Create a Post"

  1. Right-click on the `02-Working with Variables` folder → **New Request**
  2. Name it `Create a Post`
  3. Set the method to **POST**
  4. Set the URL to: `{{baseUrl}}/posts`
  5. Go to the **Body** tab → select **JSON**
  6. Paste the following:

  ```json
  {
    "title": "My First Bruno Post",
    "body": "Hello from the Bruno Live Workshop!",
    "userId": {{defaultUserId}}
  }
  ```

  7. Click **Send** ▶️
  8. You should get **201 Created** — the API created your post!

  💡 `{{defaultUserId}}` comes from the Workshop environment. Check the environment editor to see its value.

  ---

  ### 🔀 Step 2: Create "Echo with Variables"

  1. Create a new request named `Echo with Variables`
  2. Set the method to **POST**
  3. Set the URL to: `{{echoUrl}}`
  4. Go to **Body** → **JSON** and paste:

  ```json
  {
    "workshop": "{{appName}}",
    "attendee": "{{$randomFullName}}",
    "email": "{{$randomEmail}}",
    "role": "{{$randomJobTitle}}"
  }
  ```

  5. Click **Send** ▶️ — the echo server returns exactly what was sent
  6. **Send it again** — notice the dynamic values (`$randomFullName`, etc.) change each time!
  7. **Switch to the Workshop-Alt environment** and send again — notice `{{appName}}` changes too!

  ---

  ### ✏️ Step 3: Create "Update a Post"

  1. Create a new request named `Update a Post`
  2. Set the method to **PUT**
  3. Set the URL to: `{{baseUrl}}/posts/1`
  4. Go to **Body** → **JSON** and paste:

  ```json
  {
    "id": 1,
    "title": "Updated from Bruno Workshop",
    "body": "This post was updated during the live workshop!",
    "userId": {{defaultUserId}}
  }
  ```

  5. Click **Send** ▶️
  6. You should get **200 OK** with the updated post data.

  ---

  ### 📌 Summary

  In this section, you learned how to:

  - ✅ Send POST and PUT requests with JSON bodies
  - ✅ Use environment variables (`{{baseUrl}}`, `{{defaultUserId}}`, `{{appName}}`)
  - ✅ Use dynamic variables (`{{$randomFullName}}`, `{{$randomEmail}}`, `{{$randomJobTitle}}`)
  - ✅ Switch environments to change variable values

  📁 Check the **Solution** folder to see the completed requests.
}
