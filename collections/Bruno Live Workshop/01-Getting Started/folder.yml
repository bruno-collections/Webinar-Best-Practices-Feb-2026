info:
  name: 01-Getting Started
  type: folder
  seq: 1

request: {}

docs:
  content: |-
    # 🚀 Section 1: Getting Started

    👋 Welcome to the **Bruno Live Workshop!**

    In this section, you will:

    - ✅ Create your first API request
    - ✅ Send a POST request to Bruno's echo server
    - ✅ Send GET requests to public APIs
    - ✅ Explore JSON responses

    ### 📌 Prerequisites

    - Bruno is installed and running
    - You have opened this collection in Bruno
    - You have selected the **Workshop** environment from the dropdown

    ---

    ### 🌐 Step 1: Create "Hello Echo"

    1. Right-click on the `01-Getting Started` folder
    2. Select **New Request**
    3. Name it `Hello Echo`
    4. Set the method to **POST**
    5. Set the URL to: `{{echoUrl}}`
    6. Go to the **Body** tab → select **JSON**
    7. Paste the following JSON body:

    ```json
    {
      "message": "Hello from Bruno!",
      "workshop": "Live Workshop"
    }
    ```

    8. Click **Send** ▶️
    9. The echo server mirrors back whatever you send — check the response!

    ---

    ### 🐕 Step 2: Create "Get Dog Facts"

    1. Right-click on the `01-Getting Started` folder → **New Request**
    2. Name it `Get Dog Facts`
    3. Set the method to **GET**
    4. Set the URL to: `{{dogApiUrl}}/facts`
    5. Click **Send** ▶️
    6. Explore the response — each fact has an `id`, `type`, and `attributes.body`

    ---

    ### 👥 Step 3: Create "Get All Users"

    1. Right-click on the `01-Getting Started` folder → **New Request**
    2. Name it `Get All Users`
    3. Set the method to **GET**
    4. Set the URL to: `{{baseUrl}}/users`
    5. Click **Send** ▶️
    6. You'll get an array of 10 users with `name`, `email`, `address`, `company`, etc.

    💡 **Notice** the `{{baseUrl}}` in the URL — this is an **environment variable**! We'll explore these in the next section.

    ---

    ### 📌 Summary

    In this section, you learned how to:

    - ✅ Create new requests in Bruno
    - ✅ Send POST requests with JSON bodies
    - ✅ Send GET requests to public APIs
    - ✅ Read and explore API responses

    📁 Check the **Solution** folder to see the completed requests.
  type: text/markdown
